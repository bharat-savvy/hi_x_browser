import 'package:flutter/material.dart';


class BingPage extends StatelessWidget {
  const BingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
